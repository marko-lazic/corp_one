[workspace]
resolver = "2"

exclude = [
    "benches",
]
members = [
    "corp_client",
    "corp_server",
    "corp_shared",
    "tests",
    "tools/ci",
]
default-members = [
    "corp_client",
    "corp_server",
    "corp_shared",
    "tests",
]

[workspace.dependencies]
bevy = { version = "0.14.0", default-features = false }
